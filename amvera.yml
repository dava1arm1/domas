meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands: npx prisma generate && npm run build
run:
  command: npm run start
  containerPort: 3000
  servicePort: 80
